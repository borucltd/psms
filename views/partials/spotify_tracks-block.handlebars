<div>
    <input type="checkbox" name="track" value="{{ track.id }}"> {{ track.name }} ({{ track.album.artists.[0].name }})
    <img src={{ track.album.images.[0].url }} width=64 height=64 >
    <br>      
</div>